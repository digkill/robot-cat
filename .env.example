# S3 хранилище (Beget)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_DEFAULT_REGION=ru1
AWS_BUCKET=your-bucket-name
AWS_ENDPOINT=https://s3.ru1.storage.beget.cloud
AWS_USE_PATH_STYLE_ENDPOINT=true

# LLM API (OpenAI-совместимый)
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=your_api_key
LLM_MODEL=gpt-4o-mini

# Подсветка дисплея: GPIO26 (pin 37). Пусто = отключено
DISPLAY_BACKLIGHT_PIN=26

# Камера для детекции: USB-камера через OpenCV/V4L2
CAMERA_DETECTION=opencv
CAMERA_INDEX=0
CAMERA_ROTATE_180=true
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
CAMERA_FPS=15
CAMERA_JPEG_QUALITY=80

# Локальный веб-интерфейс и стрим камеры
WEB_AUTO_START=true
WEB_HOST=0.0.0.0
WEB_PORT=5000

# Remote backend (Go)
REMOTE_BRIDGE_ENABLED=false
REMOTE_BRIDGE_URL=wss://backend.example.com/ws/robot
REMOTE_BRIDGE_TOKEN=
REMOTE_ROBOT_ID=robot-cat
REMOTE_PING_INTERVAL=15
REMOTE_STATUS_INTERVAL=5
REMOTE_VIDEO_MODE=webrtc

# Лицо проверяем чаще, но приветствие не чаще раза в 10 минут
PERSON_INTERVAL=2
# Резервная фраза, если LLM недоступен
PERSON_GREETING_TEXT=Добрый день! Хорошего вам настроения!
PERSON_GREETING_COOLDOWN=600
MOTION_COOLDOWN=5
DETECTION_EVENT_COOLDOWN=5

# Слушать ответы: сек записи, мин. символов для ответа
LISTEN_DURATION=6
LISTEN_MIN_TEXT=2

# Звук: default = через PipeWire
AUDIO_DEVICE=default
AUDIO_CARD_INDEX=0
AUDIO_OUTPUT_VOLUME=100

# Снимки камеры по таймеру: 0 = отключено
SNAPSHOT_INTERVAL=0
SNAPSHOTS_UPLOAD_TO_S3=true

# TTS: provider = openai | auto | espeak | gtts
TTS_PROVIDER=openai

# OpenAI TTS
# Варианты голосов: alloy, ash, ballad, coral, echo, fable, nova, onyx, sage, shimmer, verse, marin, cedar
TTS_OPENAI_MODEL=gpt-4o-mini-tts
TTS_OPENAI_VOICE=alloy

# Локальный fallback (espeak)
# Примеры голосов: ru, en, en-us, en-gb, de, fr-fr, es, pl
TTS_VOICE=ru
TTS_SPEED=120
TTS_VOLUME=100
TTS_GAIN=2.5

# Персонаж, который говорит
# Варианты: robot_cat, assistant, guard, professor, pirate
ASSISTANT_CHARACTER=robot_cat

# Wake word (локально, без токенов)
WAKE_WORD_ENABLED=false
WAKE_WORD_PHRASE=Hello Kitty
WAKE_WORD_ALIASES=Kitty,Hello Katie,Hey Kitty,Hi Kitty
WAKE_WORD_CHUNK_SEC=3.0
WAKE_WORD_COOLDOWN=6.0
WAKE_WORD_MATCH_THRESHOLD=0.72
# Модель: vosk-model-small-en-us-0.15 (англ.) или vosk-model-small-ru (рус.)
# Если "Hello Kitty" не распознаётся — попробуйте "Kitty" или русскую модель
